import numpy as np
import matplotlib.pyplot as plt
from mpl_toolkits.mplot3d import Axes3D

# Create a meshgrid for the wireframe plot
N = 15  # Same as in your example
x = np.linspace(-10, 10, N)
y = np.linspace(-10, 10, N)
X, Y = np.meshgrid(x, y)

# Evaluate the Griewank function over the grid
Z = Griewangk([X, Y])

# Create the 3D plot
fig = plt.figure(figsize=(15, 15))
ax = fig.add_subplot(111, projection='3d')

# Plot the wireframe of the Griewank function
ax.plot_wireframe(X, Y, Z, rstride=1, cstride=1, color='blue', alpha=0.5, label='Griewank Function')

# Plot the minimizer path from the last iteration
xx = minimizersQ3[-1, :, 0]  # x1 coordinates
yy = minimizersQ3[-1, :, 1]  # x2 coordinates
zz = bestsQ3[-1, :]  # Function values along the path
ax.plot3D(xx, yy, zz, 'r-', label='Minimizer Path')  # Plot path as a red line
ax.plot3D(xx, yy, zz, 'ro', label='Path Points')  # Plot points on the path

# Highlight start and end points
ax.scatter([xx[0]], [yy[0]], [zz[0]], c='green', s=100, label='Start')
ax.scatter([xx[-1]], [yy[-1]], [zz[-1]], c='red', s=100, label='End')

# Set view angle (similar to your example)
ax.view_init(65, -40)  # elevation, azimuth

# Set labels
ax.set_xlabel('x1')
ax.set_ylabel('x2')
ax.set_zlabel('f(x1, x2)')
ax.legend()

# Show the plot
plt.show()


CODE FOR Q3, MODIFIED WITH INSPIRATION FROM Q1. TRY USING THE SAME WIREFRAME PLOTTING FOR ALL QUESTIONS. SAVE ORIGINAL CODE JUST IN CASE.

